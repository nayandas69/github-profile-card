# ============================================================
# GitHub Profile Card - Environment Variables
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# ============================================================

# -----------------------------------------------
# REQUIRED: GitHub Personal Access Token
# -----------------------------------------------
# Used to query the GitHub GraphQL API for user profiles.
#
# How to get it:
#   1. Go to https://github.com/settings/tokens
#   2. Click "Generate new token (classic)"
#   3. Select scopes: "read:user" and "repo" (for public repo data)
#   4. Generate and copy the token
#
# Or use a fine-grained token:
#   1. Go to https://github.com/settings/personal-access-tokens/new
#   2. Set permissions: "Account" -> "Read-only" for Profile
#   3. Generate and copy the token
#
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# -----------------------------------------------
# OPTIONAL: Upstash Redis (for caching)
# -----------------------------------------------
# Provides a distributed cache layer so responses are fast
# even on cold starts. Without these, only in-memory caching is used.
#
# How to get it:
#   1. Go to https://console.upstash.com/
#   2. Create a new Redis database (free tier is fine)
#   3. Copy the REST URL and Token from the database details page
#
# IMPORTANT: Free-tier Upstash databases are deleted after 14 days
# of inactivity. Use the included GitHub Action (upstash-keep-alive.yml)
# to prevent this by pinging your database every 24 hours.
#
UPSTASH_REDIS_REST_URL=https://xxxxxxxx.upstash.io
UPSTASH_REDIS_REST_TOKEN=AxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxE=

# -----------------------------------------------
# OPTIONAL: Server Port (for local development)
# -----------------------------------------------
# The port the dev server listens on. Defaults to 3000.
#
# PORT=3000

# -----------------------------------------------
# OPTIONAL: Max In-Flight Requests
# -----------------------------------------------
# Limits the number of concurrent requests to the GitHub API.
# Adjust this based on your expected traffic and GitHub API rate limits.
# MAX_IN_FLIGHT_REQUESTS=100
